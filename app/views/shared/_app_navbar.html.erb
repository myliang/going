<div class="ui left fixed vertical menu">
  <div class="item">
    <!--<a href="/"><img class="ui image" src="/logo2.png"></a>-->
    <div class="ui grey header"><i class="lightning icon"></i><%= t("logo") %></div>
  </div>
  <% user_role_rules.each do |k, v| %>
  <a class="item <% if request.fullpath.start_with? "/#{k.to_s.tableize}" %>active<%end%>" href="/<%= k.to_s.tableize %>">
    <%= t "activerecord.models.#{k.to_s.underscore}" %>
  </a>
  <% end %>
</div>

<div class="ui top fixed large menu" style="z-index: 100; padding-left: 15rem; padding-right: 1rem;">

  <div class="ui right dropdown item"><%= current_user.email %> <i class="dropdown icon"></i>
    <div class="menu">
      <a class="item" href="/my/pwd">Reset Password</a>
      <a class="item" href="/users/sign_out">Exit</a>
    </div>
  </div>
</div>